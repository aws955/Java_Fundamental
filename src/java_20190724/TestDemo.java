package java_20190724;

//import java_20190724.aa.Test;
//다른 패키지의 경우 import 를 활용하여 선언할 수 있다.
//하지만 클래스 자체가 default 인 경우에는 사용할 수 없다. (public 인 경우에만 import 가능)

public class TestDemo {
	public static void main(String[] args) {
		
		/*
		Test t ; //클래스를 선언하기 위해서는 접근 한정자가 public 이어야한다.
		Test t = new Test(); //생성자(뒤쪽의 new)의 오류 문제는 두가지 (생성자의 접근 한정자가 default 이거나 해당 생성자가 없는것)
		Math m ; //클래스 자체는 public 이기때문에 선언이 가능하다.
		Math m = new Math(); //Math클래스는 모든 생성자가 static 으로 되어있다.
		*/
	}

}
